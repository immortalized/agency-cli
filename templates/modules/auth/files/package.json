{
  "name": "__PROJECT_SLUG__-tools",
  "private": true,
  "scripts": {
    "auth:build": "docker compose --profile tools build auth-tool",
    "auth:init": "docker compose --profile tools run --rm auth-tool keys init",
    "auth:rotate": "docker compose --profile tools run --rm auth-tool keys rotate"
  }
}