{
  "name": "__PROJECT_SLUG__-tools",
  "private": true,
  "scripts": {
    "auth:build": "docker compose --profile tools build auth-tool",
    "auth:init": "docker compose --profile tools run --rm --build auth-tool keys init",
    "auth:rotate": "docker compose --profile tools run --rm --build auth-tool keys rotate",
    "auth:admin:create": "docker compose --profile tools run --rm --build auth-tool users create-initial-admin"
  }
}